# Core RAG pipeline
sentence-transformers>=2.2.0   # includes CrossEncoder for re-ranking
faiss-cpu>=1.7.4
numpy>=1.24.0
transformers>=4.37.0
torch>=2.1.0
groq>=0.31.0  # Citation-grounded cloud generation backend (uses GROQ_API_KEY)

# PDF ingestion (pdfplumber = primary; pypdf = fallback)
pdfplumber>=0.10.0
pypdf>=4.0.0
gradio_client>=2.6.1  # Optional OvisOCR2 fallback for PDF pages without usable text

# Hybrid retrieval
rank-bm25>=0.2.2

# TF-IDF sandbox / baseline
scikit-learn>=1.3.0
